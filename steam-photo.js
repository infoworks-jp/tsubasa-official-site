(()=>{'use strict';const VERSION='TSUBASA_STEAM_V6';const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const hero=document.querySelector('.hero'),wrap=document.querySelector('.ramen-wrap'),img=wrap&&wrap.querySelector('.ramen');if(!hero||!wrap||!img)return;hero.querySelectorAll('.steam-photo').forEach(n=>n.remove());const canvas=document.createElement('canvas');canvas.className='steam-photo';canvas.setAttribute('aria-hidden','true');canvas.dataset.engine=VERSION;Object.assign(canvas.style,{position:'absolute',inset:'0',width:'100%',height:'100%',zIndex:'3',pointerEvents:'none',mixBlendMode:'screen',opacity:'.78'});hero.appendChild(canvas);const ctx=canvas.getContext('2d',{alpha:true});if(!ctx)return;let dpr=1,lastW=0,lastH=0,start=performance.now();const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));function sizeCanvas(){const r=hero.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,1.6);const w=Math.max(1,Math.round(r.width*dpr)),h=Math.max(1,Math.round(r.height*dpr));if(w!==lastW||h!==lastH){canvas.width=w;canvas.height=h;lastW=w;lastH=h}}addEventListener('resize',sizeCanvas,{passive:true});function puff(x,y,rx,ry,a){const g=ctx.createRadialGradient(x,y,0,x,y,Math.max(rx,ry));g.addColorStop(0,`rgba(248,248,244,${a})`);g.addColorStop(.34,`rgba(235,236,232,${a*.55})`);g.addColorStop(.68,`rgba(218,220,216,${a*.18})`);g.addColorStop(1,'rgba(210,212,208,0)');ctx.save();ctx.translate(x,y);ctx.scale(rx/Math.max(rx,ry),ry/Math.max(rx,ry));ctx.translate(-x,-y);ctx.fillStyle=g;ctx.beginPath();ctx.arc(x,y,Math.max(rx,ry),0,Math.PI*2);ctx.fill();ctx.restore()}function draw(now){sizeCanvas();const hr=hero.getBoundingClientRect(),ir=img.getBoundingClientRect();ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,hr.width,hr.height);if(!ir.width||!ir.height){requestAnimationFrame(draw);return}const x0=ir.left-hr.left,y0=ir.top-hr.top,t=(now-start)/1000,baseX=x0+ir.width*.52,baseY=y0+ir.height*.315;ctx.save();ctx.globalCompositeOperation='source-over';const streams=[{o:-.09,s:0.00,w:1.00},{o:.015,s:1.8,w:.82},{o:.10,s:3.6,w:.65}];for(const st of streams){const count=reduced?5:8;for(let i=0;i<count;i++){const phase=(t*(reduced?.05:.12)+i/count+st.s*.07)%1;const rise=phase*ir.height*.43;const fade=Math.sin(Math.PI*phase);const sway=Math.sin(t*(.52+st.w*.08)+i*1.7+st.s)*ir.width*.018*(.6+phase*1.5);const drift=(phase-.25)*ir.width*st.o*.45;const x=baseX+ir.width*st.o+sway+drift;const y=baseY-rise;const rx=ir.width*(.030+.045*phase)*st.w;const ry=ir.width*(.055+.090*phase)*st.w;const alpha=.10*fade*(1-phase*.45)*st.w;puff(x,y,rx,ry,alpha)}}ctx.restore();requestAnimationFrame(draw)}function init(){sizeCanvas();requestAnimationFrame(draw)}if(img.complete&&img.naturalWidth)init();else img.addEventListener('load',init,{once:true})})();