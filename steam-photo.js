(()=>{'use strict';const VERSION='TSUBASA_STEAM_V11';const hero=document.querySelector('.hero'),stage=document.querySelector('.ramen-stage'),img=stage&&stage.querySelector('.ramen');if(!hero||!stage||!img)return;hero.querySelectorAll('.steam-photo').forEach(n=>n.remove());const c=document.createElement('canvas');c.className='steam-photo';c.dataset.engine=VERSION;c.setAttribute('aria-hidden','true');hero.appendChild(c);const g=c.getContext('2d',{alpha:true});if(!g)return;let dpr=1,W=0,H=0,start=performance.now();const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;function size(){const r=hero.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,1.5);const w=Math.max(1,Math.round(r.width*dpr)),h=Math.max(1,Math.round(r.height*dpr));if(w!==W||h!==H){c.width=w;c.height=h;W=w;H=h}}addEventListener('resize',size,{passive:true});function mist(x,y,rx,ry,a){g.save();g.translate(x,y);g.scale(1,ry/rx);const q=g.createRadialGradient(0,0,0,0,0,rx);q.addColorStop(0,`rgba(249,248,244,${a})`);q.addColorStop(.38,`rgba(244,243,239,${a*.42})`);q.addColorStop(.75,`rgba(236,235,232,${a*.08})`);q.addColorStop(1,'rgba(236,235,232,0)');g.fillStyle=q;g.beginPath();g.arc(0,0,rx,0,Math.PI*2);g.fill();g.restore()}function draw(now){size();const hr=hero.getBoundingClientRect(),ir=img.getBoundingClientRect();g.setTransform(dpr,0,0,dpr,0,0);g.clearRect(0,0,hr.width,hr.height);if(!ir.width){requestAnimationFrame(draw);return}const t=(now-start)/1000,bx=ir.left-hr.left+ir.width*.515,by=ir.top-hr.top+ir.height*.338,s=Math.max(.72,Math.min(1.08,ir.width/625));g.globalCompositeOperation='screen';for(let stream=0;stream<2;stream++){for(let i=0;i<5;i++){const seed=stream*13.17+i*2.61;const speed=reduced?.02:.043+stream*.004;const p=(t*speed+i/5+stream*.31)%1;const rise=p*145*s;const life=Math.sin(Math.PI*p);const sway=(Math.sin(t*.21+seed+p*4.1)+.22*Math.sin(t*.13+seed*.61+p*7.8))*8*s*(.2+p*.75);const spread=(stream?8:-8)*s*(.18+p*.5);const x=bx+spread+sway,y=by-rise;const rx=(7+p*13)*s,ry=rx*(1.8+p*.75);const a=.0125*life*(1-p*.34)*(stream?1:.82);mist(x,y,rx,ry,a)}}requestAnimationFrame(draw)}function init(){size();requestAnimationFrame(draw)}img.complete&&img.naturalWidth?init():img.addEventListener('load',init,{once:true})})();