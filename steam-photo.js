(()=>{'use strict';const VERSION='TSUBASA_STEAM_V10';const hero=document.querySelector('.hero'),stage=document.querySelector('.ramen-stage'),img=stage&&stage.querySelector('.ramen');if(!hero||!stage||!img)return;hero.querySelectorAll('.steam-photo').forEach(n=>n.remove());const c=document.createElement('canvas');c.className='steam-photo';c.dataset.engine=VERSION;c.setAttribute('aria-hidden','true');hero.appendChild(c);const g=c.getContext('2d',{alpha:true});if(!g)return;let dpr=1,W=0,H=0,start=performance.now();const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;function size(){const r=hero.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,1.5);const w=Math.max(1,Math.round(r.width*dpr)),h=Math.max(1,Math.round(r.height*dpr));if(w!==W||h!==H){c.width=w;c.height=h;W=w;H=h}}addEventListener('resize',size,{passive:true});function mist(x,y,rx,ry,a){g.save();g.translate(x,y);g.scale(1,ry/rx);const q=g.createRadialGradient(0,0,0,0,0,rx);q.addColorStop(0,`rgba(248,247,242,${a})`);q.addColorStop(.32,`rgba(244,243,238,${a*.52})`);q.addColorStop(.72,`rgba(235,234,230,${a*.12})`);q.addColorStop(1,'rgba(235,234,230,0)');g.fillStyle=q;g.beginPath();g.arc(0,0,rx,0,Math.PI*2);g.fill();g.restore()}function draw(now){size();const hr=hero.getBoundingClientRect(),ir=img.getBoundingClientRect();g.setTransform(dpr,0,0,dpr,0,0);g.clearRect(0,0,hr.width,hr.height);if(!ir.width){requestAnimationFrame(draw);return}const t=(now-start)/1000,bx=ir.left-hr.left+ir.width*.515,by=ir.top-hr.top+ir.height*.335,s=Math.max(.72,Math.min(1.08,ir.width/625));g.globalCompositeOperation='screen';for(let stream=0;stream<3;stream++){for(let i=0;i<6;i++){const seed=stream*11.71+i*2.37;const speed=reduced?.025:.055+stream*.004;const p=(t*speed+i/6+stream*.19)%1;const rise=p*185*s;const life=Math.sin(Math.PI*p);const sway=(Math.sin(t*.27+seed+p*4.7)+.28*Math.sin(t*.17+seed*.53+p*8.4))*14*s*(.25+p*.8);const spread=(stream-1)*16*s*(.25+p*.75);const x=bx+spread+sway,y=by-rise;const rx=(10+p*19)*(stream===1?1:.82)*s,ry=rx*(1.65+p*.8);const a=.026*life*(1-p*.28)*(stream===1?1:.72);mist(x,y,rx,ry,a)}}requestAnimationFrame(draw)}function init(){size();requestAnimationFrame(draw)}img.complete&&img.naturalWidth?init():img.addEventListener('load',init,{once:true})})();